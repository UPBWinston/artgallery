<form method="post" action="{{ path('app_intensity_delete', {'id': intensity.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ intensity.id) }}">
    <button class="btn">Delete</button>
</form>
