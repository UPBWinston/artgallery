{% extends 'base.html.twig' %}

{% block title %}SaleEvent{% endblock %}

{% block body %}
    <h1>SaleEvent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sale_event.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ sale_event.name }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ sale_event.date ? sale_event.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_sale_event_index') }}">back to list</a>

    <a href="{{ path('app_sale_event_edit', {'id': sale_event.id}) }}">edit</a>

    {{ include('sale_event/_delete_form.html.twig') }}
{% endblock %}
