{% extends 'base.html.twig' %}

{% block title %}SaleEventEntry{% endblock %}

{% block body %}
    {% include 'default/nav.html.twig' %}

    <h1>SaleEventEntry</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sale_event_entry.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_sale_event_entry_index') }}">back to list</a>

    <a href="{{ path('app_sale_event_entry_edit', {'id': sale_event_entry.id}) }}">edit</a>

    {{ include('sale_event_entry/_delete_form.html.twig') }}
{% endblock %}
